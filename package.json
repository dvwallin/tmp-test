{
  "name": "platform-engineer-level-3",
  "version": "1.0.0",
  "scripts": {
    "build": "echo build your docker-compose environment here",
    "start": "echo start your docker-compose envirnoment here",
    "build-flask-app": "cd app && pip3 install -r requirements.txt",
    "start-flask-app": "cd app && export FLASK_APP=main.py && python3 -m flask run",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^12.17.1"
  }
}
